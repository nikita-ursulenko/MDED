# 🚀 MDED: Готовый сайт на WordPress

**MDED** — это готовый сайт на WordPress, построенный с нуля на современных технологиях. Он отличается чистой архитектурой, высокой производительностью и адаптивным дизайном, что делает его идеальной отправной точкой для вашего проекта.

## ✨ Особенности

- **Чистая архитектура:** Чёткое разделение логики и представления благодаря шаблонизатору **Timber (Twig)**.
- **Высокая производительность:** Оптимизированный код обеспечивает быструю загрузку страниц.
- **Гибкость контента:** Интеграция с **ACF (Advanced Custom Fields)** позволяет легко управлять контентом.
- **Полная адаптивность:** Дизайн «из коробки» адаптируется под любые устройства и разрешения экрана.

---

## 🛠️ Технологический стек

- **WordPress** – CMS, удобная для пользователей и клиентов.
- **Timber / Twig** – Отделение логики от шаблонов, читаемый шаблонный код.
- **Tailwind CSS** – Утилитарный CSS с полной кастомизацией.
- **ACF** – Гибкие поля для контента и админки.
- **Composer** – Менеджер PHP-зависимостей.
- **npm / PostCSS** – Сборка и кастомизация стилей.

---

## 📂 Структура проекта

Корневая директория **MDED** содержит все необходимые файлы для работы сайта:

- `assets/` – Скомпилированные файлы для фронтенда (CSS, JavaScript, изображения).
- `node_modules/` – JavaScript-зависимости.
- `src/` – Исходники для разработки (SCSS, JavaScript и другие ассеты).
- `vendor/` – PHP-зависимости, управляемые Composer.
- `views/` – **Twig-шаблоны** (`.twig`) для отрисовки частей сайта.
- `composer.json` и `composer.lock` – Файлы Composer для управления PHP-зависимостями.
- `functions.php` – Главный файл темы.
- `index.php` – Точка входа в тему.
- `package.json` и `package-lock.json` – Файлы `npm` для управления JavaScript-зависимостями и скриптами.

---

## 🚀 Руководство по развёртыванию

Этот раздел содержит пошаговую инструкцию, как развернуть сайт на другом хостинге.

### Шаг 1: Требования к серверу

Убедитесь, что ваш хостинг соответствует следующим требованиям:

- **PHP:** 8.1+
- **База данных:** MySQL / MariaDB
- **Веб-сервер:** Apache или Nginx
- **Инструменты:** phpMyAdmin или доступ к MySQL

### Шаг 2: Файлы проекта

Загрузите папку **`htdocs`** на сервер. Разместите её в корневой директории вашего сайта (например: `/var/www/html/` или в папке, выделенной для вашего домена).

### Шаг 3: База данных

1.  Войдите в **phpMyAdmin**.
2.  Выберите существующую базу данных и перейдите в раздел **«Импорт»**.
3.  Загрузите файл **`dump.sql`** (экспорт базы данных проекта).
4.  После завершения импорта в базе данных появятся все необходимые таблицы.

### Шаг 4: Конфигурация

1.  Найдите и откройте файл **`wp-config.php`**.
2.  Обновите данные для подключения к базе данных, указав ваши значения:
    - `DB_NAME` – имя вашей базы данных
    - `DB_USER` – имя пользователя
    - `DB_PASSWORD` – пароль
    - `DB_HOST` – адрес хостинга

### Шаг 5: Домен (опционально)

Привяжите домен к папке **`htdocs`**. Если сайт после развёртывания «цепляется» к старому адресу, обновите ссылки вручную в таблице `wp_options` (поля **`siteurl`** и **`home`**).

### Шаг 6: Плагины

Убедитесь, что на сайте активированы следующие плагины:

- Advanced Custom Fields
- Yoast SEO
- ACF Content Analysis for Yoast SEO
- Polylang
- TailPress

### Шаг 7: Тема

Активируйте тему **MDED**.

---

### ✉️ Контакты

Если у вас есть вопросы или предложения, не стесняйтесь связаться со мной:

Email: **nikita.ursulenco@gmail.com**
